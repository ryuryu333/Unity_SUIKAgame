cd ~/Documents/UnityData/スイカゲーム

git commit -m "First Commit"

git push origin main

git checkout -b feature/FallingObjectUpdate

git branch -a

リモートの内容に強制的に合わせる
git fetch
git reset -- hard origin/master

git log --graph --all --decorate

スプライトのデータ管理の為にAddressablesパッケージをProjectにインストール
画像素材作成
スプライトをオブジェクトに反映させるコード作成
次はUI